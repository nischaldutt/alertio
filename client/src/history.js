// maintain a custom history object instead of the one maintained by BrowserRouter
import { createBrowserHistory } from "history";
// createBrowserHistory() returns a history object which we can use
export default createBrowserHistory();
